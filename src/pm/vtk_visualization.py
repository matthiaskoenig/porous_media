"""Lobulus visualization of vtk results."""
from pm.console import console
from pm.mesh_tools import MeshTimepoint, MeshTimecourse


if __name__ == "__main__":


    from pm import DATA_DIR

    # vtk_dir = DATA_DIR / "vtk" / "sim1"
    # mesh_results: MeshTimecourse.from_vtk_directory(vtk_dir=vtk_dir, show=False)

    # visualize_mesh_pyvista(mesh_results.mesh)




